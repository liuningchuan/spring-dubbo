<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- 应用配置 -->
    <dubbo:application name="dubbo-consumer"/>

    <!-- 注册中心配置，使用zookeeper注册中心暴露服务 -->
    <dubbo:registry address="zookeeper://127.0.0.1:2181" timeout="10000"/>

    <!-- 关闭服务消费方所有服务的启动检查。dubbo缺省会在启动时检查依赖的服务是否可用，
         不可用时会抛出异常，阻止Spring初始化完成。-->
    <dubbo:consumer check="false" />

    <dubbo:annotation package="com.liuning.dubbo.service"/>

    <!-- <dubbo:reference id="demoService" check="false" interface="org.apache.dub"/>-->
</beans>